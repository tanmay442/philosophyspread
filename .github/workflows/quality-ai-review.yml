name: Deprecated AI Review Workflow (disabled)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "This workflow has been merged into .github/workflows/build-fallow.yml"
