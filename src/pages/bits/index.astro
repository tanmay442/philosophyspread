---
export const prerender = true;

// Render the first archive page at the canonical collection root instead of
// emitting a prerendered meta-refresh redirect.
return Astro.rewrite('/bits/page/1/');
---
